import { defineConfig } from 'vite';

// `base: './'` keeps asset URLs relative so the built output works
// when served from any path (GitHub Pages project sites, subfolders, etc.).
export default defineConfig({
  base: './',
});
